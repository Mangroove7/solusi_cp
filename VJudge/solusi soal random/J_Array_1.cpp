#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N,K;cin >> N >> K;
    int l[N];
    for (int i = 0; i < N; i++) {
        cin >> l[i];
    }
    int cnt = 0;
    for(auto)

    return 0;
}